Read `.claude/workflow/checkpoint-impl.md` and follow those instructions exactly to write a checkpoint to PROGRESS.md.

Do not improvise or abbreviate. The shared implementation is authoritative. This command is a thin wrapper so that `/checkpoint` and the auto-pause path inside `/launch` produce identical checkpoints.

After following the shared instructions, give me the one-line summary described there.
